-- Finance Module Migration
-- This migration adds the accounting system tables

-- Note: Run this with: make migrate-dev
-- Or manually: docker-compose exec backend npx prisma migrate dev --name add_finance_module
